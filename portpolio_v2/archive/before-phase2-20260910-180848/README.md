# 포트폴리오 V2 메인 (고딕 기본) + 히어로 서체 비교 장면

처음에는 히어로 서체 비교(명조 A / 고딕 B)용으로 시작했고, 2026-09-09 사용자 결정으로 **고딕(sans)이 기본**이 되면서 이 폴더가 V2 메인 구현 자리가 됐습니다. `claude-draft`는 시안 보관용으로 그대로 둡니다.

## 실행

```bash
cd /Users/yangsoonmin/vibe_project/portpolio/portpolio_v2
python3 -m http.server 8790
```

- 메인(고딕, 기본): http://localhost:8790/hero-compare/sans.html
- 명조판(프리셋 비교용): http://localhost:8790/hero-compare/serif.html
- 서체 비교 인덱스(참고): http://localhost:8790/hero-compare/
- 상세 6건: `case-arpina.html`(01) · `case-eum-webops.html`(02) · `case-sangji.html`(03) · `case-donga.html`(04) · `case-cms.html`(05) · `case-ops.html`(06) — 예: http://localhost:8790/hero-compare/case-donga.html

## 구조

| 파일 | 내용 |
|---|---|
| `shared/body.template.html` | 메인의 공통 마크업. `__VARIANT__ __TITLE__ __TAG__ __FONTS__` 치환 → `sans.html`, `serif.html` |
| `build.sh` | 위 치환을 실행 (`cd hero-compare && ./build.sh`). **sans.html/serif.html은 직접 고치지 말고 템플릿을 고친 뒤 빌드**. css/js 링크에 `?v=시각`을 붙여 브라우저 캐시를 깬다(수정 후 반드시 빌드) |
| `shared/compare.css` | 메인 공통 스타일. 맨 위 **서체 프리셋 블록**(토큰 6개)으로 서체 교체. 잉크 블루 두 단계 토큰 |
| `shared/compare.js` | 오프닝: 진행도 p 하나로 hero 물러남 → 문장 정렬 → 헤드라인 → 연결 → 아르피나 장면. 하단: 사진 패럴랙스 · 영수증 인쇄 · 사진 조명 · 연락 선 |
| `case-*.html` 6건 + `shared/case.css` · `case.js` | 상세 페이지. 머리(종이) → [근거(밤) → 설명(종이)] 쌍 2~3개 → 결과(크림, "확인된 것만") → 다음 사례(밤). 스크롤 연출 없이 등장만. case.css 하단 "02~06 추가 부품": `.cshot--fit`(원본 비율, 밑줄 %는 이미지 기준) · `.duo/.quad + .shotcard` · `.sheet`(관리표) · `.plist`(원칙 목록) · `.scope`(기능 범위 SVG) · `.chead__title--long`(긴 제목 축소) |
| `shared/assets/` | 실제 캡처. arpina-lesson-*(2026.09 촬영), donga-main · donga-med(의과대학), sangji-main/50th-hanja/50th-2010, epl-main, jira-sprint/backlog/workflow(2026-07 면접 덱 자산), eum-home, webops-builder(가린 사본), cms-*(가린 사본), kakao-qr, profile |
| `stills/` | gstack 헤드리스 정지 화면 (index.html 서체 비교용) |

## 메인 흐름 (sans.html) — 히어로 + 사례 01·02·03 "한 화면에 사례 하나" (2026-09-09 밤)

경과: 39(z축 카메라) "난잡" → 44(상지 잔상 문법) "갈수록 더 난잡" → Codex 45 D안(큰 제목 → 화면 확대 → 판단) + Claude 의견 → 정지 구도 3장 시안 → 사용자 **"너무 크고 눈에 다 안 들어온다. 한 화면에 하나의 정보가 다 들어오는 게 낫다"** → 현재 구조. (판정 대기)

- 히어로 sticky 단독 **280svh**(진행 180svh, 속도·연출 불변). `.case--first { margin-top: -20svh; z-index: 2 }`로 사례 01이 히어로 마지막 20svh 위로 올라온다. 상단 `.case__rule`이 히어로 정렬선(x 5vw–95vw)을 이어받고 사례 사이 구분선이 된다.
- 사례 섹션(`.case`) = **100svh 한 화면**, 세로 가운데 정렬 2단 그리드(5fr/7fr). 왼쪽 `.case__text`: 눈썹 · 제목(히어로보다 한 단계 작게) · 역할 · `배경 / 내 판단 / 결과` dl · "사례 읽기". 오른쪽 `.case__proof`(최대 520px, 사용자 "사진이 너무 커" 반영 2026-09-10): 실제 화면의 정책 부분(`.cs__frame`의 `--x/--y/--w/--h`로 같은 캡처의 표시 범위만, 아르피나 x .215–.55 · y .15–.585 → 본문 글자 약 14px) + 잉크 블루 밑줄·메모 3개 + 캡션. 밑줄은 도착 0.9s 뒤. **02는 동아대(사용자 결정 2026-09-10: "이음을 강조하지 말자", B안)** — 제목 "약 200개의 사이트를, / 검수할 수 있는 구조로.", 대표 홈페이지 캡처 한 장. 03은 2010 장면 한 장(Codex 46). 이음+WebOps Builder는 목록 06행으로(04 통합 CMS · 05 협업 운영 · 06 이음). 상세 페이지 번호·다음 사례 연결도 같은 순서로 갱신.
- 효과는 들어올 때 한 번: 섹션이 35% 보이면 `.is-in`(올라오며 나타남) + `.is-set`(밑줄·메모 순서대로). 잔상·종이 전환·겹침·sticky 없음. 스크롤은 사례 사이에서만.
- 모바일 ≤860: 세로 스택, 01은 모바일 캡처 확대(`.cs__frame--m`). 동작 줄이기: 즉시 표시.
- 길이(1440×900): 문서 8.9화면(히어로 2.8 + 사례 3 + 04–06·About·Contact). 콘솔 오류 0, 390 가로 넘침 없음.
- 내비 `.nav`: 크림 + `mix-blend-mode: difference`(흰 캡처·종이 위에서도 읽힘).
- 폐기: 39 z축 카메라·통과 라벨, 44 잔상·두 슬롯·무리 안무, 45 사례별 종이 설명 면, 정지 3장 시안(거터~거터 큰 캡처·히어로급 제목 — 사용자 "너무 크다"). 원칙: **한 화면에 정보 하나가 통째로**, 크게 만들지 않는다.

## 결정 사항 (2026-09-09)

- 서체: 고딕 기본. 명조는 프리셋으로 남김(`data-variant="serif"`).
- 히어로 문장: "흩어진 요구를 한 줄의 정책으로, / 팀이 오픈할 수 있게." + "매장 운영 약 12년 · IT 서비스기획 2년 9개월".
- 전체 참여 프로젝트 목록은 별도 섹션 없이 About 경력 행 아래 `details/summary` 펼침(기본 접힘, Codex 33 합의). 아리모아 8 · 핸디 6 · 개인 8. FLOWON은 사례·목록 모두 제외. 상지 50주년은 상지 항목 안에 함께 표기. **영수증 효과 = V1 이식**: "펼치기"를 누르면 검은 슬롯(LED 점멸)에서 영수증 종이(RECEIPT 머리글·번호·TOTAL·찢긴 아랫단)가 1.7초 동안 뽑혀 나오고 두 번 펄럭인 뒤 미세하게 흔들린다. 닫으면 되감긴다. 경력 3줄은 즉시 표시.
- 효과 원칙(Codex 37): 히어로 문법을 섹션마다 다른 대상에 한 번씩. 큰 sticky는 히어로 하나, 상지만 짧은 고정 구간. 문의 카드 이동·메일 주소 흩뜨리기·숫자 롤링·와이어 도형·불씨는 쓰지 않는다.
- 개인 장면은 "사용자(이음 앱) / 운영자(직접 만든 WebOps Builder)" 한 쌍. 데스크톱 휴대폰 232px + 콘솔 나머지, 모바일은 세로 스택.
- 통합 CMS 행 도식은 공통 블록 1 + 추가 블록 2(예약·신청). "기능 범위를 재구성한 도식"으로 표기.
- 캡처 옆 촬영 시점·단서 문장 없음. 확인되지 않은 수치·후기·로고 없음.

## 아직 안 만든 것 / 대기

- (완료 2026-09-09) `shared/assets/webops-builder.png` = 문의·신고 화면의 **가린 사본**(문의 본문·id·계정 영역을 이미지에서 불투명 처리). 원본 3장은 `shared/_raw/`(git 제외)에만 둔다. 개요·관리 앱 목록 화면은 다른 앱 이름·수치가 보여 사용하지 않음.
- (완료 2026-09-09) 통합 CMS 행 썸네일 = `cms-board-thumb.png`(게시판 관리 화면 왼쪽 크롭). 가린 사본 4장 `cms-board/menu/content/schedule.png`(로고·"HANDY CMS"·계정·서비스명·브라우저 잔상 제거)는 통합 CMS 상세용. 원본은 `_raw/`. 기능 범위 도식 SVG는 템플릿 주석에 보관. 관리자 화면을 더 캡처하거나 로그인을 요구하지 않는다(사용자 결정).
- (진행 중, 2026-09-09 밤) "한 화면에 사례 하나" 구조(위 "메인 흐름") — **사용자 판정 대기**. 통과 시 02·03 소재·문안 확정 → 04–06·About·Contact 위계 정리 → 실기 스크롤 확인.
- (구) Codex 39 2단계 메모: 아르피나 상세 머리 짧은 오프닝(120–140svh, 제목+캡처 1개) → 나머지 5건, 04–06 썸네일 얕은 깊이 도착, About 입구·Contact 제목. 실기(실제 마우스·트랙패드 스크롤, 정지·역방향, 동작 줄이기 실기)는 사용자 확인 대기.
- (완료 2026-09-09 밤, **사용자 확인 전**) 상세 5건 `case-eum-webops/sangji/donga/cms/ops.html`. 메인 연결: 02·03 장면에 "사례 읽기", 04–06 행은 행 전체가 링크(`.row__link`, hover/터치에서 "사례 읽기 →" 표시), About 펼침 목록의 상지·동아대·CMS·이음·WebOps 항목 → 해당 상세. 다음 사례 순환 01→02→…→06→01. 헤드리스(1440·390) 등장·밑줄 위치·링크 무결성·콘솔 오류 0 확인, 실기 미확인.
- 선택 효과(핵심 장면 확인 뒤 결정): 목록 hover 큰 미리보기, Contact 글자 응용. 실제 마우스 스크롤 손맛·동작 줄이기 실기 검증은 미실행(헤드리스 프레임·정적 CSS 검토만).
- 실제 마우스 감속·스크롤 손맛 검증(헤드리스 프레임 단위만 확인).
