# 포트폴리오 V2 — Claude 구현안 (claude-main, Phase 2 내용 기반 · 2026-09-10)

2026-09-10 사용자 실행 승인에 따라 메인과 상세 6건을 개편했다. 고딕 `sans.html`이 기본이며 명조는 비교용으로 유지한다. 기존 V1과 `claude-draft`는 수정하지 않았다. 이번 작업 직전 소스는 `../archive/before-phase2-20260910-180848/`에 보존했다.

## 실행과 수정

이미 실행 중인 로컬 주소: http://localhost:8790/hero-compare/sans.html

서버가 꺼졌다면 다음과 같이 실행한다.

```sh
cd /Users/yangsoonmin/vibe_project/portpolio/portpolio_v2
python3 -m http.server 8790
```

- 메인 정본: `shared/body.template.html`, `shared/compare.css`, `shared/compare.js`
- 상세 정본: `case-*.html`, `shared/case.css`, `shared/case.js`
- 수정 후 `bash /Users/yangsoonmin/vibe_project/portpolio/portpolio_v2/hero-compare/build.sh`
- `sans.html` / `serif.html`은 생성 결과다. 직접 수정하지 않는다. 빌드는 스타일·스크립트의 캐시 버전도 갱신한다.
- `index.html` / `stills/`는 이전 서체 비교 자료다. 현재 구현의 최신 화면을 뜻하지 않는다.
- 실제 화면 자산은 `shared/assets/`를 사용한다. `_raw/` 원본은 공개 파일에 연결하지 않는다.

## 화면과 내용

히어로: **흩어진 요구와 일을 정리해, / 팀이 오픈할 수 있게.**

기존 흩어진 요구 → 기준 정렬 연출을 유지한다. 정책 설계뿐 아니라 고객 협의와 내부 조율까지 문구에 포함했다. AI는 보조 설명과 개인 프로젝트에서 다룬다.

| 순서 | 사례 | 드러낼 판단 | 상세 |
|---|---|---|---|
| 01 | 아르피나 | 기존·신규 회원 요구를 접수 정책으로 구체화, 오픈과 검수 | case-arpina.html |
| 02 | 동아대 | 기존 Google Sheets 관리표 개편, 진척 가시성·검수·우선순위 | case-donga.html |
| 03 | 상지건축 | 고객과 합의를 쌓고 내부 작업 시간 확보, 50주년 자료 분석 | case-sangji.html |
| 04 | 통합 CMS | 공통 기능과 고객별 요구 분리 | case-cms.html |
| 05 | 핸디 협업 | 점수화의 부담을 확인하고 중단, 이유 설명과 실무 지원 | case-ops.html |
| 06 | 이음 + WebOps Builder | 사용자 앱과 운영 콘솔을 직접 구현·검증 | case-eum-webops.html |

대표 3건은 같은 제목·본문·결과·실제 화면 그리드로 구성한다. 이어지는 3건은 행 전체가 상세 링크다. About의 경력과 8/6/8건 프로젝트 목록을 보존했고, Contact에는 이메일·QR과 QR에서 확인한 오픈채팅 직접 링크를 제공한다.

하단과 상세는 같은 종이색 배경, 얇은 구분선, 잉크 블루 강조를 사용한다. 상세는 설명 바로 다음에 해당 화면·도식을 배치한다. 별도의 대화문 장면과 반복되는 어두운 전환 장면은 제거했다.

## 동작과 접근성

- 큰 고정 구간은 히어로 하나(280svh). 첫 사례는 그 안에서 올라오며 정렬선을 이어받는다. 이후는 자연 스크롤이다.
- 대표 사례 버튼과 Work는 `#feature`에 도착한다. 첫 사례가 숨겨져 있을 때는 내부 링크를 비활성화하고, 이동 완료 후 접근할 수 있다.
- 모바일(860px 이하), 높이 680px 미만, 동작 줄이기 설정, 첫 사례 내용이 화면 높이를 초과하는 환경에서는 고정 연출 없이 자연스럽게 읽힌다.
- 스크립트가 없어도 본문과 프로젝트 펼침을 읽을 수 있는 기본 CSS를 둔다.
- 사례 등장·밑줄은 한 번. 사진 조명은 포인터에만 반응한다. 영수증은 열 때 인쇄 후 한 번 움직이며 계속 흔들리지 않는다.
- 관리표만 자체 가로 스크롤을 허용하며, 넘칠 때 키보드로 접근할 수 있다.

## 사실 표현의 경계

- 동아대는 Excel 또는 Google Sheets 신규 도입 사례가 아니다. 기존 Google Sheets 체계를 개편한 사례다. 표의 사이트명·담당·수치는 설명용 재구성이며 실제 운영 데이터가 아니다.
- 핸디는 스토리 포인트를 정착시킨 성공 사례가 아니다. 반복 논의와 적용 시도 후 구성원 부담을 고려해 중단한 판단이다.
- 상지 리뉴얼 약 5개월 계획 → 약 1년 진행은 사용자 진술이다. 기존 참여 기간과 전체 계약 기간의 정확한 관계는 추가 확인 전이므로 종료 월을 추정하지 않고 리뉴얼 2023 / 50주년 기획 2024로 표기한다.
- 상지 50주년 합동 발표의 경험을 전체 프로젝트 무수정 승인으로 확대하지 않는다.
- 정성 결과에 임의 개선율을 붙이지 않는다. 이음의 출시 전 상태는 유지한다.
- 현재 공개 화면 캡처만으로 과거 정책의 모든 세부가 검증된다고 주장하지 않는다.

검증 범위와 제한은 `PHASE2-REVIEW.md`에 기록한다. 배포는 수행하지 않았다.

## Claude 구현안 (이 폴더, 2026-09-10 저녁) — Phase 2 내용 위에 디자인만

사용자 결정: "Claude도 구현하고 Codex 구현과 비교" (Codex 안은 `../hero-compare/`, Codex 릴레이 48). 이 폴더는 Phase 2 파일 복사본이며 `shared/assets`는 hero-compare 자산의 심볼릭 링크(새 파일 추가만).

- 사례 01~03 **밤 배경 복귀**, 사례 하나 = **100svh 한 화면**(세로 중앙), 사례 사이 빈 구간 0(01은 히어로 sticky 끝에서 인계 — Phase 2 방식, 02·03은 바로 이어짐). 종이는 04~06부터. 히어로는 정렬(종이) 뒤 인계 구간(p .76–.90)에서 다시 밤으로 돌아와 01과 이어진다. 내비 종이 판정은 히어로 종이 구간과 `#work` 이후.
- 이미지: 사례당 한 장. **01 = 직접 디자인한 Figma 시안**(`shared/assets/arpina-figma-lesson.png`, 필터+강습 카드 영역 y .225–.95, 밑줄 3 = 접수 중 신청하기 / 접수 대기 D-12 / 신청 마감; 캡션에 "설계 시안" 표기 + 가이드의 재등록 18~22일 → 신규등록 25일~말일 순서 명시). 02 = 대표 홈페이지, 03 = 2010 장면. 텍스트 5 : 이미지 7.
- 효과: 사례당 하나 — 본문·메타는 제자리 페이드(.6s), 이미지는 정착(scale .97→1, .8s) → 밑줄 순서(.is-set 350ms 뒤, 지연 .18/.36). 첫 사례는 인계가 등장을 대신. 모바일·동작 줄이기는 즉시 표시.
- CSS는 compare.css 끝 "Claude 구현안" 블록이 Phase 2 규칙을 덮어쓴다(되돌리기 쉽게). JS 변경 2곳(mix 복귀, paintNav).
- 실측(헤드리스): 1440×900 사례 3개 각 900px·사이 간격 0·문서 7.15화면, 1280×720 01 내용 542px(고정 연출 유지), 390 가로 넘침 없음, 콘솔 오류 0. 실기 스크롤·Safari·실기기는 미검증.
